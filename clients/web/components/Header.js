import React, { Component } from 'react';

/**
 * The header component.
 */
export default class Header extends Component {
  render() {
    return (
      <header>
        Header
      </header>
    );
  }
}
