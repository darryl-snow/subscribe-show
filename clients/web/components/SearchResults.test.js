import React from 'react'
import { shallow } from 'enzyme'
import SearchResults from './SearchResults'

describe('SearchResults Component', () => {
  it('should render the component', () => {
  })
  it('should sort the list', () => {
  })
  it('should filter the list', () => {
  })
  it('should update the list controls', () => {
  })
  it('should return results based on the query', () => {
  })
  it('should sort by airDate by default', () => {
  })
  it('should be sorted in ascending order by default', () => {
  })
})
