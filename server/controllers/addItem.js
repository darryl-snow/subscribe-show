module.exports = function(req, res, next) {
  // get show id
  // get show details from tmdb
  // get all episodes from tmdb
  // call addItem Mutation
  // for each episode, call addEpisode mutation
  // respond with show + episode list
}
